# OAuth 2.0 Configuration - Frontend
# Ces variables sont nécessaires pour générer les URLs d'autorisation OAuth côté client

# Google OAuth 2.0 - Client ID (public, pas de secret côté client)
# Obtenir depuis Google Cloud Console > Credentials > OAuth 2.0 Client IDs
VITE_GOOGLE_CLIENT_ID=123456789-abcdefghijklmnopqrstuvwxyz.apps.googleusercontent.com

# 42 School OAuth 2.0 - Client ID (public, pas de secret côté client)
# Obtenir depuis profile.intra.42.fr > Settings > API > Applications
VITE_SCHOOL42_CLIENT_ID=ID 

# Note: Les secrets OAuth ne doivent JAMAIS être exposés côté client
# Ils sont uniquement configurés côté backend dans .env.auth