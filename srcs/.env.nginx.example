# =============================================================================
# OAuth 2.0 - Variables Frontend (côté client, valeurs PUBLIQUES)
# =============================================================================
# Ces variables sont intégrées au bundle JavaScript par Vite au moment du BUILD.
# Elles sont PUBLIQUES : ne jamais mettre de secrets ici.
#
# Usage : Copier ce fichier en `.env.nginx` et renseigner vos vraies valeurs.
#   cp srcs/.env.nginx.example srcs/.env.nginx
#
# Obtenir les credentials :
#   Google  → https://console.cloud.google.com/ > Credentials > OAuth 2.0
#   42 School → https://profile.intra.42.fr/ > Settings > API > Applications
#
# Redirect URIs à configurer chez les providers :
#   Google  → https://localhost:4430/auth/oauth/google/callback
#   42      → https://localhost:4430/auth/oauth/school42/callback
# =============================================================================

# Google OAuth 2.0 - Client ID (public)
VITE_GOOGLE_CLIENT_ID=123456789-abcdefghijklmnopqrstuvwxyz.apps.googleusercontent.com

# 42 School OAuth 2.0 - UID Application (public)
VITE_SCHOOL42_CLIENT_ID=u-s4t2ud-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
